# dsh-plugin-management 图标白名单（Reicon Outline · 2026-09-11）
| 语义 | 导出名 | 形态(fill/stroke) | 使用位置 |
|---|---|---|---|
| 核心不可停用 | lock | fill | 插件列表「核心」标签 |
| 回顾到期提醒 | alert-circle | fill | 回顾提醒横幅 |

> Reicon Outline 为 fill 式（根 `<svg fill="none">` + path `fill="currentColor"`），path 的 `fill="currentColor"` 不可省略；若内联为 `<symbol>` 雪碧图需补 CSS `.ic{fill:currentColor}`。
